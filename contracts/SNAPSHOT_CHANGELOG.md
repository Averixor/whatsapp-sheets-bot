## 2026-05-22 — baseline

- stage7-usecases-facade.json: initial PR6 contract (17 methods); G1 governance metadata (`schemaRef`, `reviewedAt`, `changeReason`)
- access-debug-baseline.json: schema-only placeholder; `descriptorSchema` moved to `contracts/access.contract.json`
