## Результат

- **Перевірка репозиторія:** усі перелічені у `clasp` `.gs`/скрипти на місці; **`node scripts/ci-gas-sanity.mjs`** зараз проходить (`ok`, 92 `.gs` файли).
- **Аналіз функцій:** повного «усі використовуються» з коду немає (тригери, HTML `google.script.run`, меню тощо); **реально підозріла:** `_applyFailedAuth_` у `AccessControl.Mutations.gs` ніде не викликається.
- **`npm run ci`** у попередній сесії **не завершився** через системне повідомлення про вимкнений prompt («Press any key») — сам скрипт при цьому **виконується через `node` без проблем**.

## Що зроблено зараз

- Підтверджено роботу саніті-чека через **`node`**.

## Якщо захочете наступний крок

- Підключити виклики **`_applyFailedAuth_`** у гілках невдалої автентифікації в resolver/enforcement або свідомо прибрати/задокументувати мертвий код.